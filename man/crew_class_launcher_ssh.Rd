% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crew_ssh.R
\name{crew_class_launcher_ssh}
\alias{crew_class_launcher_ssh}
\title{\code{ssh} launcher class}
\description{
\code{R6} class to launch and manage \code{crew} workers on a remote
system over \code{ssh}.
}
\details{
Workers are ordinary \code{crew} workers started with \code{Rscript} on the
remote system. They reach the local \code{mirai} dispatcher through a reverse
\code{ssh} tunnel (\code{ssh -R}), so the remote system never needs to open a
connection to the local machine directly and the local machine needs no
inbound firewall rule or public address.

The launcher shells out to the system \code{ssh} client rather than using an
in-process \code{ssh} library, so \verb{~/.ssh/config} entries, \code{ssh-agent},
\code{known_hosts}, \code{ProxyJump}, and preshared keys all behave exactly as they
do on the command line. Authentication must be non-interactive
(\code{BatchMode=yes} is used), which in practice means key-based
authentication.
}
\examples{
if (identical(Sys.getenv("CREW_SSH_EXAMPLES"), "true")) {
controller <- crew_controller_ssh(ssh_host = "user@example.com")
controller$start()
controller$push(Sys.info()[["nodename"]])
controller$wait()
controller$pop()
controller$terminate()
}
}
\seealso{
Other plugin_ssh:
\code{\link[=crew_controller_ssh]{crew_controller_ssh()}}
}
\concept{plugin_ssh}
\section{Super class}{
\code{\link[crew:crew_class_launcher]{crew::crew_class_launcher}} -> \code{crew_class_launcher_ssh}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-crew_class_launcher_ssh-initialize}{\code{crew_class_launcher_ssh$new()}}
    \item \href{#method-crew_class_launcher_ssh-validate}{\code{crew_class_launcher_ssh$validate()}}
    \item \href{#method-crew_class_launcher_ssh-start}{\code{crew_class_launcher_ssh$start()}}
    \item \href{#method-crew_class_launcher_ssh-settings}{\code{crew_class_launcher_ssh$settings()}}
    \item \href{#method-crew_class_launcher_ssh-launch_worker}{\code{crew_class_launcher_ssh$launch_worker()}}
    \item \href{#method-crew_class_launcher_ssh-logs}{\code{crew_class_launcher_ssh$logs()}}
    \item \href{#method-crew_class_launcher_ssh-terminate}{\code{crew_class_launcher_ssh$terminate()}}
  }
}
\if{html}{\out{<details><summary>Inherited methods</summary>
<ul>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="call"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-call'><code>crew::crew_class_launcher$call()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="crashes"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-crashes'><code>crew::crew_class_launcher$crashes()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="launch"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-launch'><code>crew::crew_class_launcher$launch()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="launch_workers"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-launch_workers'><code>crew::crew_class_launcher$launch_workers()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="poll"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-poll'><code>crew::crew_class_launcher$poll()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="scale"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-scale'><code>crew::crew_class_launcher$scale()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="set_name"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-set_name'><code>crew::crew_class_launcher$set_name()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="crew" data-topic="crew_class_launcher" data-id="terminate_workers"><a href='../../crew/html/crew_class_launcher.html#method-crew_class_launcher-terminate_workers'><code>crew::crew_class_launcher$terminate_workers()</code></a></span></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-initialize}{}}}
\subsection{\code{crew_class_launcher_ssh$new()}}{
  Create an \code{ssh} launcher object.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$new(
  ...,
  ssh_host,
  ssh_port = NULL,
  ssh_keyfile = NULL,
  ssh_options = character(0L),
  ssh_command = "ssh",
  rscript = "Rscript",
  launch_prefix = character(0L),
  directory = NULL,
  remote_directory = "/tmp",
  verbose = FALSE
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Arguments passed to
\code{crew::crew_class_launcher$initialize()}.}
      \item{\code{ssh_host}}{Character of length 1, the \code{ssh} destination of the
remote system, e.g. \code{"user@example.com"}. Anything the \code{ssh} command
accepts works here, including a \code{Host} alias from \verb{~/.ssh/config}.}
      \item{\code{ssh_port}}{Positive integer of length 1 or \code{NULL}, the port of the
remote \code{ssh} server (\code{ssh -p}). \code{NULL} uses the \code{ssh} default.}
      \item{\code{ssh_keyfile}}{Character of length 1 or \code{NULL}, path to the private
key file to authenticate with (\code{ssh -i}). \code{NULL} uses the keys \code{ssh}
would choose on its own.}
      \item{\code{ssh_options}}{Character vector of additional arguments for the
\code{ssh} command, e.g. \code{c("-o", "ProxyJump=bastion")}. These are placed
ahead of the options the launcher sets, and \code{ssh} honors the first
occurrence of an option, so anything given here takes precedence.}
      \item{\code{ssh_command}}{Character of length 1, the \code{ssh} executable to run.}
      \item{\code{rscript}}{Character of length 1, path to \code{Rscript} on the remote
system. The default assumes \code{Rscript} is on the remote \code{PATH} for
non-interactive \code{ssh} sessions.}
      \item{\code{launch_prefix}}{Character vector, a command and its arguments to
run the worker under, e.g.
\code{c("docker", "run", "--rm", "--network=host", "my-image")}. Empty by
default, which runs \code{rscript} directly. Each element is quoted for the
remote shell separately, so an element may contain spaces without
being split into two arguments.}
      \item{\code{directory}}{Character of length 1 or \code{NULL}, working directory of
the workers on the remote system. \code{NULL} means the directory \code{ssh}
lands in, usually the remote home directory. This is the directory the
command named by \code{launch_prefix} starts in, which for a container
runtime is not the same as the working directory inside the container.}
      \item{\code{remote_directory}}{Character of length 1, absolute path to a
directory on the remote system where the launcher may write worker
scripts and logs. Each controller gets its own uniquely named
subdirectory of \code{remote_directory}.}
      \item{\code{verbose}}{Logical of length 1, whether to print messages about the
tunnel and each worker launch.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An \code{ssh} launcher object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-validate"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-validate}{}}}
\subsection{\code{crew_class_launcher_ssh$validate()}}{
  Validate the launcher.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$validate()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{NULL} (invisibly). Called for its side effect of throwing an
error if the launcher is misconfigured.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-start"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-start}{}}}
\subsection{\code{crew_class_launcher_ssh$start()}}{
  Start the launcher and open the reverse \code{ssh} tunnel that
remote workers use to reach the local \code{mirai} dispatcher.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$start(url = NULL, profile = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{url}}{Character of length 1, \code{mirai} URL of the local dispatcher.}
      \item{\code{profile}}{Character of length 1, \code{mirai} compute profile.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{NULL} (invisibly).
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-settings"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-settings}{}}}
\subsection{\code{crew_class_launcher_ssh$settings()}}{
  \code{mirai} daemon settings for a remote worker.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$settings()}
    \if{html}{\out{</div>}}
  }
  \subsection{Details}{
    Identical to the settings of the parent class except that the
URL points at the remote end of the reverse \code{ssh} tunnel instead of
the dispatcher's local address.
  }
  \subsection{Returns}{
    A named list of \code{mirai} daemon settings.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-launch_worker"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-launch_worker}{}}}
\subsection{\code{crew_class_launcher_ssh$launch_worker()}}{
  Launch a worker on the remote system.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$launch_worker(call)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{call}}{Character of length 1, R code the worker should run.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A handle: a named list with the worker \code{name}, the remote
process ID \code{pid}, and the remote paths of the worker \code{script} and
\code{log}.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-logs"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-logs}{}}}
\subsection{\code{crew_class_launcher_ssh$logs()}}{
  Read the remote log files of the workers this launcher
started.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$logs(lines = 100L)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{lines}}{Positive integer of length 1, number of trailing lines to
read from each log file.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Details}{
    Worker output is the first place to look when a worker starts
but never connects back to the controller. The logs live on the remote
system and outlive the workers, so this method still works after a
worker crashes.
  }
  \subsection{Returns}{
    A character vector of log file contents, with a header line
naming each file. Returns \code{character(0)} if the launcher has not
started any workers.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-crew_class_launcher_ssh-terminate"></a>}}
\if{latex}{\out{\hypertarget{method-crew_class_launcher_ssh-terminate}{}}}
\subsection{\code{crew_class_launcher_ssh$terminate()}}{
  Terminate the launcher: stop remote workers and close the
reverse \code{ssh} tunnel.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{crew_class_launcher_ssh$terminate()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    \code{NULL} (invisibly).
  }
}

}
